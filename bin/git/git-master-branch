#!/bin/sh

# No arguments are allowed
if [ "$#" -ne 0 ]
then
	echo >&2 "usage: $(basename -- "$0" | sed 's/-/ /1')"
	echo >&2
	echo >&2 "Add changes in and push them up to the remote"
	exit 80
fi

git symbolic-ref refs/remotes/origin/HEAD -q
